# 需求与反馈池 (Backlog & Feedback Pool)

这里用于收集所有来自用户的真实使用反馈、吐槽以及功能优化建议。
我们将基于“用户体验驱动”的思想，定期对这里的反馈进行整理（Triage），并归纳合并到未来的迭代版本（Roadmap）中进行开发落地。

---

## 📥 新增收集池 (Inbox)
*在此处记录日常收集到的零散反馈和最初始的想法，等待评估...*

- [ ] (示例) **用户反馈**: CLI 初始化项目时，如果网络不佳，下载模板时没有明确的进度状态，感觉卡住了。
- [ ] (示例) **功能建议**: 现有生成的 Vue 模板能不能支持自定义全局 Error Handler 的注入口？
- [ ] (示例) **体验优化**: 每次更新 Schema 重新 `generate` 后，如果控制台能更清晰地打印出“到底合并/新增了哪些字段、哪些代码块被热缝合保留了”的 Diff 摘要就好了，现状是一闪而过。

---

## 🤔 需求整理与架构设计 (Triage & Design)
*已经被确认是有价值的反馈，正进入技术可行性分析和底层方案设计阶段...*

- [ ] **进一步丰富 Web UI 控制台功能**
  - **背景**：当前 UI 已支持查看路由与组件，但缺乏深度交互。
  - **建议方案**：支持在 UI 上直接修改 Schema 内容并触发保存，实现 Web IDE 的初步雏形。

---

## 📅 版本规划与迭代进度 (Roadmap)

### 🎯 候选版本：v3.5.x 「极致体验优化与可视化感知」
*当前聚焦：以修复和体验增强为主*
* [ ] 优化 `npx fe-factory generate` 命令终端日志，加强增量编译的 Diff 感知能力。
* [ ] 完善本地自动化测试和自检自愈(`doctor` 命令)对于边缘情况的错误容错度。
* [ ] 优化生成出来的组件注册表逻辑。
* [ ] CLI 新增 `--dry-run` 空运行命令，仅打印会生成的文件而不做真正写入。

### 🎯 候选版本：v3.6.x 「智能可视化引擎与生态进阶」
*当前聚焦：打破终端黑盒，提供所见即所得的体验，并增强数据回流*
* [ ] 进一步扩展 `skills/` 下沉淀的最佳实践逻辑链。
* [ ] `npx fe-factory report` 报告深度接入 Lighthouse 性能评分数据。

---

## ✅ 历史已交付 (Done / Archived)
* [x] ~~**优化应用项目 UI 入口感知**~~：在所有官方模板中内置 `factory:ui` 脚本，并在实战教程中新增“步骤 4”引导进入 Web UI。 (v3.6.2)
* [x] ~~**深入优化 CLI 命令终端体验**~~：引入轻量级 Spinner 动画，优化耗时任务状态反馈。 (v3.6.1)
* [x] ~~`npx fe-factory report` 生成的周报接入本地真实的 `eslint`/`typescript` 告警数据，真实反哺给开发人员。~~ (v3.6.0)
* [x] ~~💻 **Web UI 控制台 (`fe-factory ui`)**：为底层项目提供独立的图形化管理面板 (Visual Dashboard)，直观呈现组件复用图谱与路由网络。~~ (v3.6.0)
* [x] ~~🖱️ **前端可视化点击生成**：将晦涩的 `--schema` 路径长命令封装到 UI 面板中，支持在 Web 端点选已有 Schema 图纸进行“一键雷达生成”。~~ (v3.6.0)
* [x] ~~📖 **文档重构**：全面重新梳理《5 分钟速通实战》的连贯体验，消除新手的割裂感。~~ (v3.5.0)
* [x] ~~🔧 **Admin 模板增强**：为 `vue3-element-admin` 和 `react-antd-admin` 加入开箱即用的多环境配置（`.env.development / .test / .production`）与 Axios BaseURL 动态切换支持。~~ (v3.5.0)
* [x] ~~修复 `npx fe-factory init` 等命令中 Inquirer 版本不兼容导致终端选择项不显示的问题。~~ (v3.5.0)
* [x] ~~为全量流程文档补充结合 Agent (如 Cursor / Windsurf) 的 AI一键工作流图纸生成能力。~~ (已于教程补充)
